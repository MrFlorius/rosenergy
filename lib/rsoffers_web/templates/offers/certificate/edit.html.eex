<h1>Edit Certificate</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.offers_certificate_path(@conn, :update, @certificate)) %>

<span><%= link "Back", to: Routes.offers_certificate_path(@conn, :index) %></span>
