<%= form_for @changeset, @action, fn f -> %>
  <%= if @changeset.action do %>
    <div class="alert alert-danger">
      <p>Oops, something went wrong! Please check the errors below.</p>
    </div>
  <% end %>

  <%= label f, :precentage %>
  <%= number_input f, :precentage, step: "any" %>
  <%= error_tag f, :precentage %>

  <div>
    <%= submit "Save" %>
  </div>
<% end %>
